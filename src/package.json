{
  "name": "frontend-template-nuxt",
  "version": "0.0.1",
  "description": "An application to set or customize favorite colors.",
  "author": "Sheryl Tania",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "lint": "eslint --cache --ext .ts,.js,.vue --ignore-path ../.gitignore .",
    "test": "jest",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@byu-oit/nuxt-common": "^2.16.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "fs": "0.0.1-security",
    "net": "^1.0.2"
  },
  "//peerDependenciesComment": "The 'peerDependencies' section is only to help IDE autocompletion. It should only list a few necessary sub-dependencies from @byu-oit/nuxt-common",
  "peerDependencies": {
    "@nuxtjs/axios": "*",
    "@vue/test-utils": "*",
    "nuxt-property-decorator": "*",
    "vuex": "*",
    "vuex-mock-store": "*"
  },
  "devDependencies": {
    "@types/node": "^18.6.1"
  }
}
